# Copy this file to `.env` and fill in values as needed.
# Vite will expose variables prefixed with VITE_ to the client code.

# URL where your API server is running. For local dev, keep it as below.
VITE_SERVER_URL=http://localhost:4000

# Gemini (ai.google.dev) API key, required for OCR extraction.
# Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: pick a model you have access to. Common public models:
# - gemini-1.5-flash
# - gemini-1.5-flash-8b
# - gemini-1.5-pro (requires access)
GEMINI_MODEL=gemini-1.5-flash

# Optional: Mapbox token for geocoding extracted addresses.
# Get one at https://account.mapbox.com/
MAPBOX_GEOCODING_TOKEN=

# Optional: MongoDB connection string to persist extracted records.
MONGODB_URI=
MONGODB_DB=
